apiVersion: v1
kind: Service
metadata:
  name: {{ include "automation-factory.fullname" . }}-frontend
  labels:
    {{- include "automation-factory.frontend.labels" . | nindent 4 }}
  {{- with .Values.frontend.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "automation-factory.frontend.selectorLabels" . | nindent 4 }}
